 

#include<stdio.h>
#include<conio.h>

void main(){
    int n,mul;
    printf("Enter the no. : ");
    scanf("%d", &n);

    for (int i = 0; i < 11; i++)
    {
        /* code */
        mul = n*i;
        printf("%d X %d = %d\n", n, i, mul);
    }
    
    getch();
}
